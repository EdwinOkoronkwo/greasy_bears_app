import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'greasy_bears_app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
